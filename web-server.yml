---
  - name: "Test hello world"
    hosts: awx-client
    remote_user: jdoe
    become: true

    tasks:
      - name: "Get ls output and print"
        shell: "ls -ltrh"
        register: ls

      - debug: ls.stdout