---
  - name: "Test hello world"
    hosts: awx-client.localdomain
    remote_user: jdoe
    become: true

    tasks:
      - name: "Get ls output and print"
        shell: "ls -ltrh"
        register: ls_result

      - debug:
          var: ls_result
          verbosity: 2